package com.HotelBook.HotelBooking.HotelPolicy;

/**
 * Type of breakfast service offered by the hotel.
 * Used in BreakfastPolicy.type — null if breakfastOffered=false.
 */
public enum BreakfastType {
    CONTINENTAL,
    BUFFET,
    FULL_ENGLISH,
    AMERICAN
}
